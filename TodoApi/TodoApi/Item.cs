namespace TodoApi
{
    public class Item
    {
        // Program.cs 에 있는 의존성 주입된 app을 인스턴스를 가져오는 코드
        // TodoDb db = app.Services.GetRequiredService<TodoDb>();
        // db.Todos.Add(todo);
        // await db.SaveChangesAsync();

         
        

    }
}
